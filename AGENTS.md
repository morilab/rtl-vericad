# AGENTS.md

## 目的
- 最終目標は、VS Code 拡張の WebView 上で動作する 2D CAD を実現すること。
- 初期開発では、同一の設計思想を維持した単体ブラウザアプリで設計検証、操作性検証、デバッグを行うこと。

## 現在フェーズ
- 現在は使用検討および仕様策定フェーズとする。
- ユーザーから明示的な指示がない限り、製品実装は行わない。
- まず `spec/` 配下の文書を更新し、合意形成を優先する。

## 言語方針
- 仕様書は原則として日本語で記述する。
- コードコメント、ドキュメントコメント、設計補足コメントは原則として日本語で記述する。
- ユーザー向け UI 文言は原則として日本語で記述する。
- 外部 API 名、標準規格名、ライブラリ固有名、識別子、プロトコル項目名などは、正確性を損なう場合に無理に翻訳しない。

## ファイル命名方針
- `spec/` 配下のファイル名は、明確な理由がない限り日本語とする。
- `spec/` 配下以外では、日本語ファイル名を禁止する。
- `spec/` 配下で英語名または混在名を使う場合は、対象文書内または `spec/99-決定事項.md` に理由を残す。

## 言語運用の徹底ルール
- 新規に仕様書、コメント、UI 文言を追加または更新する際は、日本語で記述できない理由がないか先に確認する。
- `spec/` 配下へ新規ファイルを追加する際は、日本語ファイル名を既定とし、英語名や混在名は例外として扱う。
- `spec/` 配下以外へ新規ファイルを追加する際は、日本語ファイル名を使わない。
- 新規コミットのコミットメッセージは、明確な理由がない限り日本語で記述する。
- 既存文書に英語中心の記述が残っている場合は、正確性を損なわない範囲で日本語へ寄せる。
- 本方針に反する追加や更新を見つけた場合は、そのまま踏襲せず、修正または理由の明記を優先する。

## 絶対に守る設計ルール
- UI 層から Node.js API を直接使用しない。
- VS Code API は host adapter 層の外から直接呼び出さない。
- CAD コアロジックは React から独立させる。
- ドキュメントモデルは描画実装から独立させる。
- 単体ブラウザアプリで常に動作可能な構成を維持する。
- すべての編集操作は Undo/Redo 可能な構造を前提に設計する。
- document state、view state、tool state を分離する。

## 推奨レイヤ構成
- `cad-core`
  - 幾何計算
  - ドキュメントモデル
  - エンティティ定義
  - 選択
  - スナップ
  - コマンド
  - Undo/Redo 履歴
- `app-ui`
  - React コンポーネント
  - ツールバー
  - パネル
  - 入力イベントの接続
  - 画面遷移を含まない UI 制御
- `host-adapter`
  - browser host
  - VS Code WebView host
  - 永続化連携
  - メッセージ橋渡し
  - ホスト依存機能の吸収

## WebView 互換ルール
- UI コードはブラウザ互換実行環境を前提とする。
- ファイル I/O、設定保存、クリップボード連携、ホスト通信は host abstraction 経由に限定する。
- UI からローカルファイルシステムへの直接アクセスを前提に設計しない。
- WebView で制約を受けやすい機能を採用する場合は、制約と代替案を `spec/` に明記する。

## 状態モデル方針
- document state
  - エンティティ
  - レイヤ
  - メタデータ
- view state
  - ズーム
  - パン
  - ビューポート
  - 一時オーバーレイ
- tool state
  - 現在ツール
  - 操作途中状態
  - スナップ候補
  - ドラッグ状態

## 永続化方針
- 内部保存形式は JSON first を原則とする。
- SVG や DXF などの入出力形式で内部データ構造を規定しない。
- 描画表現を真実のソースにせず、ドキュメント状態から描画データを導出する。

## 依存関係方針
- ブラウザ互換の依存関係を優先する。
- 大きな依存関係を導入する場合は、必要性、責務、代替案を `spec/99-決定事項.md` に記録する。
- CAD コア層は、UI ライブラリ依存を持ち込まない。

## 文書更新方針
- アーキテクチャ判断、制約、重要な保留事項は `spec/` に記録する。
- VS Code WebView 最終目標に影響する設計判断は、理由と影響範囲を明示する。
- 新しいサブシステムを提案する場合は、必要性、責務、配置レイヤ、WebView 互換性を説明する。
